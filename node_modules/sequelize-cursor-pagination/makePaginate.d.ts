import { ModelStatic, Model } from 'sequelize';
import { MakePaginateOptions, PaginateOptions, PaginationConnection } from './types';
declare const makePaginate: <ModelType extends Model<any, any>>(model: ModelStatic<ModelType>, options?: MakePaginateOptions) => (queryOptions: PaginateOptions<ModelType>) => Promise<PaginationConnection<ModelType>>;
export default makePaginate;
